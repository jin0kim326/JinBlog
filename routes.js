const HOME = "/";
const CATEGORY = "/:category";
const ABOUT = "/about";
const CONTACT = "/contact";
const POST = "/:post";

const routes = {
  home: HOME,
  category: CATEGORY,
  about: ABOUT,
  contact: CONTACT,
  post: POST,
};

export default routes;
