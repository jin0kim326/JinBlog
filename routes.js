const HOME = "/";
const CATEGORY = "/category";
const ABOUT = "/about";
const CONTACT = "/contact";
const POST = "/:post";
const SEARCH = "/search";
const WRITE = "/write";

const routes = {
  home: HOME,
  category: CATEGORY,
  about: ABOUT,
  contact: CONTACT,
  post: POST,
  search: SEARCH,
  write: WRITE,
};

export default routes;
