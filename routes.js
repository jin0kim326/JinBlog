const HOME = "/";
const CATEGORY = "/category";
const ABOUT = "/about";
const CONTACT = "/contact";
const POST = "/:post";
const SEARCH = "/search";

const routes = {
  home: HOME,
  category: CATEGORY,
  about: ABOUT,
  contact: CONTACT,
  post: POST,
  search: SEARCH,
};

export default routes;
